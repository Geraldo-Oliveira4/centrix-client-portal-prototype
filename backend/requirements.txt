# Web framework (prototype-only; replaces AWS Lambda + API Gateway)
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0

# Copied from the Centrix backend shared/ layer dependencies
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9
aws-lambda-powertools[tracer]>=3.0
boto3>=1.34
