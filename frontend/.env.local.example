# Backend base URL. In this prototype the backend is the local FastAPI app,
# not the AWS API Gateway. Without this the portal calls its own origin and
# every request 404s.
NEXT_PUBLIC_API=http://localhost:8000
