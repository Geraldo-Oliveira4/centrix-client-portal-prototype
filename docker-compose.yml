services:
  postgres:
    image: postgres:16
    container_name: centrix_portal_pg
    environment:
      POSTGRES_PASSWORD: test
      POSTGRES_DB: centrix_portal
    ports:
      - "5432:5432"
    volumes:
      - centrix_portal_pgdata:/var/lib/postgresql/data

volumes:
  centrix_portal_pgdata:
